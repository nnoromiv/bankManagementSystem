import Authenications.Login;

public class App {
    /**
    * Entry point for the login. It is called from the command line. The main method does nothing but throws an exception if there is a problem.
    * 
    * @param args - The command line arguments to be used as the
    */
    public static void main(String[] args) throws Exception {
        Login.main(args);
    }
}
